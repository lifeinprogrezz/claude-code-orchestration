# Follow-ups — the one-way channel back

This kit is generated from a private repo and pulled here. Sync is one way: the laptop is
canonical, this machine only pulls. Nothing here is pushed anywhere.

When a lesson learned on this machine should improve the method (a hook edge case, a rule
that reads wrong, a missing skill), write it below as one line with a date. The owner carries
it back by hand.

## Open
- The CLAUDE.md size guard measures one file. It does not add `@`-imported files. Keep
  `CLAUDE.local.md` short.
- `ultracode-sweep` is not in the kit: it is bound to the GitHub CLI.

## Log
<!-- - YYYY-MM-DD — <lesson, one line> -->
